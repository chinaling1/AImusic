# 古韵AI 多智能体审核报告

> 审核时间：2026-09-09　|　审核对象：古韵AI V2.3（MiniMax 提示词工坊）
> 审核方法：**4 个独立审核代理并行只读审核**，分别从格式合规、提示词工程、代码质量、参赛合规四个维度出具结论，主代理汇总。
> 同日已完成一轮重构（见《重构变更说明.md》），报告中已标注修复状态。

---

## 总体结论

| 维度 | 结论 | 摘要 |
|---|---|---|
| A. MiniMax 格式合规 | ⚠️ 有条件通过 | 核心口径（3500/2000、英文标签、独占一行）与官方一致；校验器存在 4 处误报/漏报逻辑缺陷 |
| B. 提示词工程 | ⚠️ 有条件通过 | 领域专业性强（4/5）；鲁棒性最弱（2/5）：无异常处理、解析零容错 |
| C. 代码质量 | ⚠️ 有条件通过 | 架构清晰、校验器与 LLM 解耦是亮点；存在打包失效与密钥管理两类 P0 |
| D. 参赛合规 | ⚠️ 有条件通过 | 无一票否决项命中；但 5 项 P0 必补（成品音频、表述纠正、举证材料等） |

**P0 问题共 9 项，其中 4 项已在当日重构中修复，其余列入待确认清单。**

---

## A. MiniMax 格式合规审核

### 结论：有条件通过

核心口径与官方一致：歌词 3500 / Styles 2000 字符上限、英文结构标签独占一行、禁止中文标签。主流程可产出合规内容。

### 问题清单

| 级别 | 问题 | 证据 | 状态 |
|---|---|---|---|
| P1 | 歌词留空被判 `error`，但官方允许留空由 Styles 自动生成，报错文案自相矛盾 | `minimax.py:99-100` | ❌ 待修复（改 `warning`） |
| P1 | 纯音乐模式仍执行逐行标签校验，与"歌词应留空"提示冲突 | `minimax.py:93-141` | ❌ 待修复（`instrumental=true` 时跳过） |
| P1 | 带序号标签仅放行 6 种，`[Pre-Chorus 1]`、`[Outro 2]`、`[Interlude 1]` 被误判 | `minimax.py:24` | ❌ 待修复（正则扩展至全部标签） |
| P1 | 结构检查用子串匹配漏报：`pre-chorus` 含 `chorus` 子串 → 无副歌也不告警；`[Chorus 1]` 精确匹配失败 → 缺主歌检查被跳过 | `minimax.py:128,131` | ❌ 待修复（先归一化再判断） |
| P1 | LLM 输出无后处理：markdown 围栏 ` ``` ` 不以 `[` 开头，校验器不检测，粘贴即污染 | `lyric.py:16`、`llm_service.py:121` | ✅ **已修复**：提示词新增"禁止围栏/前后缀"条款（`prompt_library.py`），并实测输出无围栏 |
| P2 | `[Intro` 特例导致同行内容漏报；注记只认半角括号；大小写判 `error` 过重；阈值 20 与文案"80-200 字"不一致；`meta_tags` 收到但未校验；前端 `QUICK_TAGS` 8 项少于后端 17 项 | `minimax.py` 多处 | ❌ 待修复（详见《计数器规则文档》§六） |
| P2 | 前端 UTF-16 码元 / 后端码点计数单位不一致 | `LyricStep.tsx` vs `minimax.py` | ❌ 待修复（前端改 `[...str].length`） |
| P2 | 歌词超限未禁用确认/导出按钮 | `LyricStep.tsx:167`、`ExportStep.tsx:217` | ❌ 待修复 |

---

## B. 提示词工程质量审核

### 维度评分

| 维度 | 得分 | 说明 |
|---|---|---|
| 指令清晰度 | 3.5/5 | 格式条款较全，曾缺"禁止围栏"与超限压缩策略 |
| 输出稳定性 | 2.5/5 | 有 `===META===` 分隔标记，但解析正则零容错 |
| 鲁棒性 | 2/5 | 全链路无 try/except、无重试、无超时、无截断检测 |
| 可控性 | 3/5 | 押韵偏好与纯音乐模式能传导，但为软描述且有语义冲突 |
| 领域专业性 | 4/5 | 宫/羽调式、散板、轮指、泛音等术语有效，e2e 输出质量达标 |

### 问题清单

| 级别 | 问题 | 证据 | 状态 |
|---|---|---|---|
| P0 | 分隔标记解析零容错：`=== Styles ===` 等变体导致 meta/styles 双双为空且前端静默降级 | `PromptStep.tsx:8-9` | ❌ 待修复（正则放宽 + 空结果提示重试） |
| P0 | 全链路无异常处理/重试/超时：API 失败直接 500，前端弹 Internal Server Error | 后端各 router | ⚠️ **部分修复**：客户端已设 `timeout=300`；异常捕获与友好提示仍待做 |
| P0 | `generate_lyrics` 未设 `max_tokens`、未查 `finish_reason`，长歌词被截断后静默入库 | `llm_service.py` | ⚠️ **部分修复**：`call_deepseek_flash` 已设 2048；主链路 `finish_reason` 检查仍待做 |
| P1 | 纯音乐模式与"押韵工整正文"要求语义冲突 | `LyricStep.tsx:28` vs `llm_service.py:115` | ✅ **已修复**：按模式追加不同约束指令（`LYRICS_INSTRUMENTAL_EXTRA` / `LYRICS_VOCAL_EXTRA`） |
| P1 | 押韵偏好为软描述，无韵脚硬约束 | 同上 | ✅ **已修复**：提示词增加韵部硬约束条款，实测 ang 韵全篇一致 |
| P1 | `quick_suggest` 要求 ≤50 字却给 2048 tokens | `llm_service.py` | ✅ **已修复**（统一 `_chat` 后 flash 固定 2048，短输出天然受限；建议后续降至 200） |
| P1 | 未禁止 markdown 围栏与解释性前后缀 | `llm_service.py:101,121` | ✅ **已修复**（同 A-P1 围栏项） |

---

## C. 代码质量审核

### 结论：架构清晰，两类 P0 待决

亮点：确定性校验器与 LLM 解耦、主流程三步闭环完整、zustand 持久化结构清楚。

### 问题清单

| 级别 | 问题 | 证据 | 状态 |
|---|---|---|---|
| P0 | Electron 打包后 `API_BASE='/api'` 相对路径在 `file://` 下解析失败，打包版主流程全废 | `electron/main.js:47-48`、`api.ts:1` | ❌ 待确认（打包方案后修复） |
| P0 | 设置里 API Key 仅写内存，后端重启即丢；保存失败仍提示"已保存" | `main.py:40-43`、`SettingsModal.tsx:35-38` | ❌ 待确认（落盘 `.env`） |
| P1 | 复制失败零反馈 | `ExportStep.tsx:28-44` | ❌ 待确认 |
| P1 | persist 无 version/partialize：旧 localStorage 残留、刷新后错误横幅不消失 | `musicStore.ts` | ✅ **已修复**：`version:1` + `migrate`（清理 legacy 字段）+ `partialize`（排除 loading/error）+ `sessionId` 兜底 |
| P1 | `sessionId` 恒为空，后端会话能力架空 | `musicStore.ts:66` | ✅ **已修复**：初始状态即生成 `crypto.randomUUID()` |
| P1 | CORS `*` + `credentials=True` 非法组合 | `main.py:20-26` | ❌ 待确认（需先解决打包态 Origin=null） |
| P1 | 死代码约 500+ 行（兜底管线组件）、`lrc_utils.generate_lrc` 恒等函数、`result.content` 永不可达分支 | 多处 | ✅ **已修复**：`lrc_utils.py` 删除；4 个兜底组件移入 `frontend/legacy/`（git mv 保留历史）；`abcjs/tone/midi-player-js` 依赖移除；store 兜底字段清除 |
| P2 | 无用静态资产（hero.png/react.svg/vite.svg/icons.svg 零引用） | `frontend/src/assets`、`frontend/public` | ✅ **已修复**（已删除） |
| P2 | 测试脚本硬编码路径、依赖真实 LLM | `ui_test.js`、`e2e_test.py` | ❌ 待改进 |

---

## D. 参赛合规审核

### 结论：有条件通过，5 项 P0 必补

无一票否决项命中；素材合规（未引入任何第三方 SoundFont/声库/IR/商业字体，依赖均为 MIT/BSD/Apache 类许可）。

### 缺口清单

| 级别 | 缺口 | 补齐建议 | 截止 |
|---|---|---|---|
| P0 | **成品音频为零**（无 mp3/wav） | 用 MiniMax 生成主成品 + 纯器乐版并存档 | 9/20 前 |
| P0 | **"本地 LLM"表述失实**：实为云端 DeepSeek/Qwen API + 本地确定性校验 | 全部材料改用「本地提示词工程 + 云端大模型 API + 本地确定性校验算法」 | 立即（本报告发布起生效） |
| P0 | MiniMax 输出水印/元数据未检测、商用授权凭证未留存 | 订阅后首件事：生成测试曲检查水印 + 留存凭证 | 9/20 前 |
| P0 | 创作过程说明四要素（提示词 + 元标签设计 + 技术路线 + 跨平台流程）与版权声明未成文 | 按本报告 §"创作过程说明应补充字段"成文 | 9/20 前 |
| P0 | 导出提示词包举证字段不足 | 补：UTC+8 时间戳、模型名与版本、调用参数、迭代轮次与父版本、人工干预 diff 与干预率、校验器结论快照、平台与账号、成品 SHA-256 | 9/20 前 |
| P1 | 原创周期举证弱：git 提交集中在 2026-09-08~09-09 两天（12 次） | 9/10 起每日连续提交、按阶段打 tag、附 V1 前序物证（早期文档/文件 mtime） | 持续 |
| P1 | 校验器作为"原创算法"分量偏弱（格式层非音乐层） | 补音乐层规则（五声性占比、调式一致性、韵脚对齐）+ 违规率收敛曲线、一次通过率等量化指标 | 9/15 前 |
| P1 | SRS 为 V1 旧路线、演示视频缺失、源文件包未打包 | SRS 已于 2026-09-09 更新 V2.3 修订记录；视频与打包排入 9/15 后 | 9/20 前 |
| P2 | 歌词个别意象偏冷硬（"照你眉间血""碑上碣"） | 定稿时替换为「眉间雪/月」等；主题（匠心传承/非遗）方向安全 | 定稿时 |

### 创作过程说明应补充字段（照做清单）

1. UTC+8 ISO 时间戳；2. 模型名与版本（deepseek-v4-pro / qwen-plus / MiniMax Music-3.0）；3. 调用参数（temperature、thinking 开关、max_tokens、模式）；4. 迭代序号 + 父版本 ID + 每轮修改说明；5. 人工干预记录（编辑前后 diff、干预率、干预理由）；6. 校验器结论快照（ok/issues/stats）；7. 生成平台与账号归属；8. 成品音频文件名 + SHA-256 绑定。建议 JSON + PDF 双份，PDF 转 PDF/A-1b。

---

## P0 汇总与修复优先级（距 9-20 完成硬线）

| 优先级 | 事项 | 类别 |
|---|---|---|
| ① 立即 | 全部材料纠正"本地 LLM"表述 | 合规 |
| ② 本周 | MiniMax 订阅 + 测试曲查水印 + 留凭证 | 合规 |
| ③ 本周 | 出第一首成品 mp3（用本工具生成提示词 → 网页版生成） | 交付 |
| ④ 本周 | Electron 打包 API_BASE 修复（若演示用 exe） | 代码 |
| ⑤ 次周 | 校验器 P1 缺陷修复（4 项）+ 音乐层规则增强 | 算法 |
| ⑥ 次周 | 提示词解析容错 + 异常捕获 + 复制反馈 + 超限禁用 | 代码 |
| ⑦ 持续 | 每日 git 提交 + 导出包补 8 项字段 | 举证 |
| ⑧ 9/15+ | 创作过程说明 / 版权声明 / 演示视频 / PDF / 源码包 | 文档 |

---

## 数据说明

- 本报告所有代码行为结论均来自审核代理对源码的实读（文件:行号可追溯），格式口径以 MiniMax 官方 API 文档为基准。
- e2e 样例来自 2026-09-09 真实调用（`backend/tests/e2e_result.json`）。
- "已修复"标注对应提交：后端重构与前端重构（见《重构变更说明.md》中的提交哈希）。
