# 2026-09-09 工作日志

## MiniMax 提示词工具改造（V2.3）— 提交 8ac36a7

### 调研结论（MiniMax 输入格式，已实测核实）
- **网页版**（minimax.io/audio/music，Music-3.0 Creator Beta）：Lyrics 框输入 `/` 快捷插入结构标签；**标签后可加编曲/人声/情绪注记**；留空按 Styles 自动生成；有 Instrumental 开关（纯音乐模式创意全写 Styles）；Styles 支持风格标签多选
- **API 口径互证**：prompt ≤2000 字符、歌词 ≤3500 字符、14 种英文结构标签、lyrics_optimizer / is_instrumental
- **元标签模板**：[Genre][BPM][Key][Vocals][Instruments][Arrangement] 六项，对应手册「元标签设计」评分要求

### 软件改造内容
- 主流程四步 → 三步：提示词（元标签+Styles）→ 歌词（MiniMax 标签格式）→ 导出（成品提示词包）
- 后端新增 `routers/minimax.py`：`POST /api/minimax/validate` 确定性格式校验器（中英标签纠错/字符上限/结构顺序/注记格式，纯算法）+ `GET /api/minimax/format-spec`（格式规格单一事实来源）
- `llm_service.py` 两个 system prompt 全部重写为面向 MiniMax 输出格式
- 前端新增 `step3/ExportStep.tsx`（一键复制/导出 txt/校验结果/网页版操作指引）；LyricStep 去掉 LRC 时间轴，加结构标签快捷插入
- 曲谱/MIDI 本地管线代码整体保留（P1 兜底），移出主流程

### 环境事实
- 后端运行环境：系统 python `D:/Program Files/Python314/python.exe`（fastapi 0.141.1 + uvicorn + openai 已装）；本轮补装 `aiosqlite 0.22.1`、`music21 10.5.0`（--user）
- 受管 venv 无 fastapi，跑此项目后端须用系统 python
- 前端构建 `npm run build`（tsc -b 全量类型检查）通过；vite 代理 /api → localhost:8000

### 遗留
- ~~LLM 真实调用链路（DeepSeek/Qwen key 配置后）未在本轮实测~~ → **2026-09-09 晚已修复并实测通过**
- MiniMax 网页版订阅后需按 V2.2 风险项检查水印/元数据

## 模型名核实与端到端测试（V2.3.1）— 提交 e475d19

### 模型实际名称（联网核实，勿凭记忆）
- **DeepSeek 官方 api-docs.deepseek.com**：旧 `deepseek-chat` / `deepseek-reasoner` 已于 **2026-07-24 15:59 UTC 正式退役**，调用直接报错。当前官方：`deepseek-v4-pro`（贵约 3 倍，强推理）、`deepseek-v4-flash`（通用高性价比）。base_url `https://api.deepseek.com` 不变
- **Qwen 百炼**：Batch + Responses 文档均列 `qwen-plus`（长期兼容名），可继续用；另有 `qwen3.5-plus` / `qwen3.7-plus` 等更新版本
- **v4 系列默认开启 thinking 模式**：必须显式传 `extra_body={"thinking":{"type":"disabled"}}` 才能关闭，否则会拖慢 + 污染输出

### 端到端测试通过（真实 DeepSeek 调通）
- API 层 `backend/tests/e2e_test.py`：提示词→歌词→校验三步全过，10 段结构、595 字符，零问题
- UI 层 `backend/tests/ui_test.js`（playwright-core + 本机缓存 Chromium）：真实浏览器走完三步无错误，4 张截图落盘 `测试截图/`
- 环境变量 `DEEPSEEK_API_KEY` 在系统 env 已配置；Qwen 未配置（主流程不依赖）

### 端到端测试环境
- playwright-core 安装在 `C:/Users/ZI/.workbuddy/binaries/node/workspace/node_modules`
- 复用本机已缓存 Chromium：`C:/Users/ZI/AppData/Local/ms-playwright/chromium-1208/chrome-win64/chrome.exe`
- Node 运行用受管 22.22.2-2 路径

## 全面重构 + 配套文档（V2.3.2）— 提交 62f837c / 97553b7 / 8db95cf

### 重构成果（接口契约零变更）
- **后端**：system prompt 集中到 `app/prompts/prompt_library.py`；`_chat()` 统一消除三段重复调用；music21 延迟导入（启动 2.50s → 2.05s，-18%；框架固定成本 1.3s 为底座）；删除恒等函数 `lrc_utils.py`；flash max_tokens 256→2048；提示词加防围栏/韵部硬约束条款
- **前端**：移除 abcjs/tone/midi-player-js 三依赖；兜底组件 670 行移入 `frontend/legacy/`（不编译可恢复）；抽公共组件 SectionCard/CharCounter/CopyButton + useCopy；constants/limits.ts 常量化；store persist 加 version/migrate/partialize + sessionId 初始化；删零引用资产 4 件
- **验证**：build ✓ / 后端冒烟 ✓ / 真实 LLM e2e（12段461字符零问题）✓ / Playwright UI 三步 ✓ 零控制台错误

### 多智能体审核（4 代理并行，全部只读）
- 格式合规：有条件通过，校验器 4 处 P1（歌词留空判 error、纯音乐仍校验、序号标签放行不全、子串匹配漏报）→ **未修，在 minimax.py 待办**
- 提示词工程：鲁棒性最弱 2/5（解析零容错、无异常捕获）→ 部分修复
- 代码质量：P0 = Electron 打包 API_BASE 失效 + Key 仅内存 → **未修，列入待确认**
- 参赛合规：**P0 必须 9/20 前补**：①成品音频为零 ②MiniMax 水印/授权未验 ③创作过程说明四要素 ④导出包补 8 类举证字段 ⑤git 提交仅 2 天需每日连续提交补强

### ⚠️ 重要事实纠正（答辩红线）
**「本地 LLM」表述停用**。真实构成 = 本地提示词工程 + 云端 DeepSeek/Qwen API + 本地确定性校验。任何材料不得称"本地部署大模型"。

### 文档产出（文档/ 目录）
计数器规则文档.md、多智能体审核报告.md、审核展示.html、重构变更说明.md；SRS.md 更新 V2.3 修订记录

### 教训
- 子代理连续 429 限流（frontend-refactor/docs-writer ×2 全失败，至次日 08:03）→ 关键工作主代理直做；frontend-refactor 失败前已完成 90% 前端改造，接手时先盘点再动手，避免重复
- 子代理无 Bash/TaskList 工具，派单时必须明确限定工具集
